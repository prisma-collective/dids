[build]
buildCommand = "pnpm --filter @prisma-dids/dashboard... build"

[deploy]
startCommand = "cd apps/dashboard && npx next start --port ${PORT:-3000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
