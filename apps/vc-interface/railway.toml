[build]
buildCommand = "pnpm --filter @prisma-events/dids-vc-interface... build"

[deploy]
startCommand = "cd apps/vc-interface && npx next start --port ${PORT:-3000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
